/**
 * Public exports for the test harness.
 *
 * Each domain's public surface is re-exported here as its modules are
 * implemented. During Phase 1 scaffolding this module is intentionally
 * thin; richer re-exports arrive with the domain phases.
 */

export {};
